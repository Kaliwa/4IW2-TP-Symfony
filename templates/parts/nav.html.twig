    <nav class="bg-gray-800 p-4">
        <div class="max-w-7xl mx-auto flex items-center justify-between">
            <a href="{{ path('app_home') }}" class="text-white text-lg font-semibold hover:text-gray-300">Tableau de bord</a>
            <div class="flex space-x-4">
                <a href="{{ path('subject_list') }}" class="text-white hover:text-gray-300">Liste des matières</a>
            </div>
        </div>
    </nav>

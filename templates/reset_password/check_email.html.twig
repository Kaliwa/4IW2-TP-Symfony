{% extends 'base.html.twig' %}

{% block title %}E-mail de réinitialisation de mot de passe envoyé{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gray-50">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-lg">
            <p class="text-center text-gray-700">
                Si un compte correspondant à votre e-mail existe, un e-mail a été envoyé contenant un lien que vous pouvez utiliser pour réinitialiser votre mot de passe.
                Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
            </p>
            <p class="text-center mt-4">
                Si vous ne recevez pas d'e-mail, veuillez vérifier votre dossier de spam ou <a href="{{ path('app_forgot_password_request') }}" class="text-blue-600 hover:underline">réessayer</a>.
            </p>
        </div>
    </div>
{% endblock %}

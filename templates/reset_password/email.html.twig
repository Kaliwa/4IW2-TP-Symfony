{% extends 'base.html.twig' %}

{% block title %}Réinitialisez votre mot de passe{% endblock %}

{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gray-50">
        <div class="bg-white p-6 rounded-lg shadow-lg w-full max-w-lg text-center">
            <h1 class="text-2xl font-semibold text-gray-800">Bonjour !</h1>

            <p class="mt-4 text-gray-600">Pour réinitialiser votre mot de passe, veuillez cliquer sur le lien suivant :</p>

            <a href="{{ url('app_reset_password', {token: resetToken.token}) }}" class="text-blue-600 hover:underline mt-4 block">
                {{ url('app_reset_password', {token: resetToken.token}) }}
            </a>

            <p class="mt-4 text-gray-600">Ce lien expirera dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>

            <p class="mt-4 text-gray-600">À bientôt !</p>
        </div>
    </div>
{% endblock %}

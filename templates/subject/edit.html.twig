{% extends 'base.html.twig' %}

{% block title %}Edit Subject{% endblock %}

{% block body %}
    <h1>Edit Subject</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-success">Save</button>
    {{ form_end(form) }}

    <a href="{{ path('subject_list') }}" class="btn btn-secondary mt-3">Back to list</a>
{% endblock %}
